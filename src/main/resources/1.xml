<?xml version="1.0" encoding="UTF-8"?>
<solver>

    <!-- Domain model configuration -->
    <solutionClass>com.example.optaplanner_industry.example.domain.TaskAssignment</solutionClass>
    <entityClass>com.example.optaplanner_industry.example.domain.Task</entityClass>


    <!-- Score configuration -->
    <scoreDirectorFactory>
        <scoreDrl>rules.drl</scoreDrl>
    </scoreDirectorFactory>

    <!-- Optimization algorithms configuration -->
    <termination>
        <secondsSpentLimit>10</secondsSpentLimit>
    </termination>


</solver>